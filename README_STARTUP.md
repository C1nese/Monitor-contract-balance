# Startup

`start-monitor.cmd` starts the Node server in a minimized window and opens the browser at `http://127.0.0.1:3000`.

The Windows Startup folder can launch this script automatically after sign-in.
